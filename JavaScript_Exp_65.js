// ForEach Loops in Array - arr.froEach(callBackFunction) - Here, it is a function to execute for each elements in the array - A callback is a function passed as an argument to another function.

let arr = [5,1,2,3,4,]
arr.forEach(function printVal(val)  
{
  console.log(val);  
}) ;